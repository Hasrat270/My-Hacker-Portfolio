---
const links = Astro.props.links ?? [];
---
<div class="contact-row">
  {links.map((link) => (
    <a class="contact-chip" href={link.url} target="_blank" rel="noreferrer noopener">{link.name}</a>
  ))}
</div>
